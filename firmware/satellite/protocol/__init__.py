# Importing `protocol.space_packet` on the satellite uses the same source
# as the host-side tooling. Copy `../../../../protocol/space_packet.py`
# onto the Pico at this path. The `tools/deploy.sh` script does this
# automatically.
